{
  "name": "wasm-typst-studio-rs",
  "version": "0.1.0",
  "description": "Pure Rust WASM Typst Studio with Tailwind CSS 4",
  "scripts": {
    "dev": "trunk serve",
    "build": "trunk build --release",
    "build:optimized": "trunk build --release && ./scripts/compress-wasm.sh",
    "build-css": "tailwindcss -i ./tailwind.css -o ./public/styles.css --minify",
    "tauri:dev": "cargo tauri dev",
    "tauri:build": "cargo tauri build",
    "size-report": "./scripts/size-report.sh"
  },
  "devDependencies": {
    "daisyui": "^5.0.0",
    "tailwindcss": "^3.4.18"
  },
  "dependencies": {
    "@iconify-json/lucide": "^1.2.16",
    "@iconify/tailwind": "^1.1.3"
  }
}
